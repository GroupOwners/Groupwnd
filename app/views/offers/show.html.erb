<header class="page-title">
  <h1>Offer</h1>
</header>

<section>
  <%= render @offer %>
  <% if @offer.total_ammount == 0 %>
    <p> SORRY, NO MORE!!! </p>
  <% else %>
    <%= button_to "Purchase", offer_purchase_path(@offer) %>
  <% end %>
</section>
