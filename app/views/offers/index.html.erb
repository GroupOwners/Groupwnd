<h1 class="page_title">Your Coupons For Today</h1>

<ul class="nine_offers">
  <% @offers.each do |offer| %>
    <li class="one_offer">
      <%= link_to image_tag(offer.image), offer_path(offer) %>
    </li>
  <% end %>
</ul>
